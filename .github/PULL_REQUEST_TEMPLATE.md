## Summary

What does this PR do and why?

## Testing

- [ ] Ran `python -m unittest discover -v`
- [ ] Ran `python -m compileall start_gui.py nte_auto_fish tests`
- [ ] Tested manually on Windows, if behavior changed

## Checklist

- [ ] GUI design is unchanged unless the PR explicitly changes UI design
- [ ] No local `config.json` or calibration data is committed
- [ ] Docs/default config updated if behavior changed
